apiVersion: v1
kind: Service
metadata:
  name: brain-tasks-app-svc
  namespace: production
spec:
  type: LoadBalancer
  selector:
    app: brain-tasks-app
  ports:
    - port: 80
      targetPort: 3000
